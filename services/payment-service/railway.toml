[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py migrate && gunicorn payment_service.wsgi:application --bind 0.0.0.0:$PORT"
healthcheckPath = "/api/payment/"
restartPolicyType = "on_failure"
